import "./header.scss"
import Navbar from "../navbar/navbar";

const Header = () => {
    return (
        <section id="header">
            <Navbar></Navbar>
        </section>
    );
};

export default Header;